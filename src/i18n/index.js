import translations from "./translations"
import languages from "./languages"
import { detectLocale, defaultLang } from "./detectLocale"

export { translations, languages, detectLocale, defaultLang }
